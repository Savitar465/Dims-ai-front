# Backend route for the DIMS AI API (see openapi.json).
# The backend runs on port 3001; the Next frontend runs on port 3000.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Optional server-side bearer token for machine-to-machine API calls.
# API_BEARER_TOKEN=
